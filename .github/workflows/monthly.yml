name: monthly-meta-review
on:
  schedule:
    - cron: "30 9 1 * *"   # 09:30 UTC = 12:30 Kuwait, 1st of month
  workflow_dispatch: {}
permissions:
  contents: write
concurrency:
  group: paper-trader
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - run: python run_agent.py monthly
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          MODEL: ${{ vars.MODEL || 'claude-haiku-4-5-20251001' }}
      - name: Commit results
        run: |
          git config user.name "paper-trader-bot"
          git config user.email "bot@users.noreply.github.com"
          git add -A
          git commit -m "monthly meta-review $(date -u +%F)" || echo "no changes"
          git push
